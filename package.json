{
  "name": "test",
  "version": "1.0.0",
  "description": "```zsh docker stop docket-test || true && docker rm parrot-pipeline || true docker build --no-cache --force-rm -t account/imagename:tag . docker run -it --name docket-test --entrypoint=/bin/bash account/imagename:tag ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@azure/communication-react": "^1.5.0",
    "picocolors": "0.0.1"
  }
}
